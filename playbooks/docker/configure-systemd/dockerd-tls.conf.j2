[Service]
ExecStart=
ExecStart=/usr/bin/dockerd \
    -H fd:// \
    --containerd=/run/containerd/containerd.sock \
    -H tcp://0.0.0.0:2376 \
    --tlsverify \
    --tlscacert={{ cert_dest }}{{ cert_ca }} \
    --tlscert={{ cert_dest }}{{ cert }} \
    --tlskey={{ cert_dest }}{{ cert_key }}

